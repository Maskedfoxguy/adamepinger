{
  "name": "adamepinger-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "echo 'Husky setup available - run npx husky init to enable'",
    "lint": "npm run lint --prefix server && npm run lint --prefix client",
    "format": "npm run format --prefix server && npm run format --prefix client",
    "test": "npm test --prefix server && npm test --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6",
    "prettier": "^3.6.2"
  }
}
